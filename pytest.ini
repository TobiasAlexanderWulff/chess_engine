[pytest]
testpaths = tests
addopts = -q -m "not slow and not bench"
pythonpath = src
markers =
    slow: long-running tests excluded from default runs
    bench: performance or micro-benchmarks excluded from default runs
